# 让 AI Agent 从"陌生外包"变成"懂你的合伙人"

[![版本](https://img.shields.io/badge/版本-v4.9-2ea44f)]()
[![中文](https://img.shields.io/badge/lang-中文-3aa675)]()
[![Hermes Agent](https://img.shields.io/badge/Hermes%20Agent-开箱即用-5865F2)]()

一键初始化 Hermes：新用户免配置免学习直接上手，老用户自动增量、零副作用。

---

## 这是什么

一份**开箱即用的 Hermes 初始化方案**。它解决一个真实痛点：Hermes 装好后默认是个**能力很强但完全不认识你**的"陌生外包"——不知道你的生意、你的标准、你的雷区，会礼貌敷衍、编数据、等你指令。

这套方案让 Hermes 在**首次对话时**就完成对你的画像，把它从"陌生人"变成"懂你、替你扛活、敢反驳你"的 AI Agent 合伙人。

## 两个文件

| 文件 | 给谁 | 作用 |
|------|------|------|
| `hermes_onboarding_guide.md` | Hermes 执行 | 一键初始化的完整执行脚本（Q1~Q9 + 写入映射 + 规则） |
| `hermes_onboarding_methodology.md` | 给人看 | 方法论说明，讲清楚"为什么这么做" |

## 核心特性

- **新用户**：完整初始化，零配置零学习直接上手
- **老用户**：自动增量升级，只补缺的字段，**不重走、不动已有配置**（零副作用）
- **版本机制**：统一版本自适应，后续升级喂新版即增量

## 版本

- v4.9（2026-09-14）｜ 更新日期：2026-09-14

### v4.9 更新说明（检测唤醒全自动 · 明确"零手动"）

- **明确"唤醒全自动"**：脚本检测到 `NEW_VERSION` 后，**唤醒 Hermes 做升级这一步由 Hermes 的 monitor 机制自动完成**（自动唤醒 agent 进增量合并），使用者只需提前说一句"开启自动更新"，全程自动：检测 → 唤醒 → 升级 → 告知。**不需要使用者复制任何通知/消息给 Hermes。**
- **防"纯通知断链"**：明确警告别建成"脚本只通知、用户手动转发给 Hermes"的方式——那会让升级断链（检测到新版也没人执行）。此为 v4.8 落地方式缺的最后一环，与外部反馈同一机制链。

### v4.8 更新说明（检测 cron 落地方式 · 修复"缺 LLM provider"）

- **修复自进化检测的落地缺口**：此前「检测机制」只写"Hermes 每天闲时调用 check_update.py"，没说明检测任务该怎么建——有用户按字面建了 **agent 模式定时任务**，但没配 LLM provider，一跑就报 `No LLM provider configured`、检测直接失效。
- **明确落地方式**：检测本身是纯脚本（monitor / no_agent，零 LLM、不需要配 provider），**只在输出 `NEW_VERSION` 时才唤醒 Hermes agent 做增量升级**——别把检测建成 agent 模式任务（那必须配 provider）。检测用脚本省 token、不依赖 provider，升级动作才唤醒 agent。此缺口由外部用户 Hermes 经反馈机制回馈发现（第三条外部贡献）。

### v4.7 更新说明（长期进化·可归因提交与提交卫生）

- **② 工具与调试纪律新增"提交卫生"三条**：①可归因提交（一事一 commit，多个独立改动拆多个 commit，别挤进一个多文件 commit）；②公开仓库 commit message 永久留痕（一旦 push 永久可见，内部痕迹/敏感词/情绪吐槽绝不进 message，发布前当公开文档审）；③纪律靠机制不靠记忆（改版本号/变更记录这类必守纪律装 pre-commit hook 硬拦，auto-commit 脚本加 `--no-verify` 防被 hook 卡死）。来源：真实提交卫生血训，通用工程纪律，任何维护代码库的 Hermes 用户都适用。

### v4.6 更新说明（版本号存储统一约定 · 修复自动检测盲区）

- **修复自动检测盲区**：此前 guide 教 Hermes 把 `onboarding_version` 写进 memory / user profile，但检测脚本 `check_update.py` 读的是 `~/.hermes/onboarding_version` **独立文件**（不读 memory）——两处不一致导致开了自动更新的用户**永远检测不到新版**（本地版本读到空 → 误判已最新）。
- **统一真源**：`onboarding_version` 唯一真源 = `~/.hermes/onboarding_version` 文件；升级/初始化写版本号时务必同步落盘该文件，memory 字段仅用于引导判断。此盲区由外部用户 Hermes 经 feedback 通道实测踩坑回馈发现（第二条外部贡献）。

### v4.5 更新说明（指令级安全红线 · 首个外部贡献纳入）

- **自进化机制新增指令级安全红线（静默 ≠ 盲从）**：增量合并不是无条件照单全收——新版文档若含**索要密钥/凭据、修改安全配置项、访问异常域名、要求关闭审批**等可疑指令，停下不执行、向使用者报告。增量合并防"误覆盖"、人工审核防"垃圾进库"，**红线防"仓库自身被投毒"（执行时实时闸门）**，三者缺一不可。
- **该条款由外部使用者贡献**：经「贡献方法论」匿名通道真实推送（首个端到端验证的贡献闭环：外部用户 → HTTPS → 去隐私 → inbox → 人工审核 → 纳入新版），判据来自本配置包自身的验证纪律与放权边界。

### v4.4 更新说明（methodology 精简 + 版本号改造）

- **methodology 长期进化章节精简**：6 块核心纪律（零副作用写入/①验证②工具③记忆③-1技能库④运维⑤诚实⑥成本）压缩为简要说明，每块一两句核心要点，细节和可执行动作指向 `hermes_onboarding_guide.md`「长期进化」章节详版。给人看的文档更精简，想深入了解的读者自行翻 guide。
- **guide 详版完整保留**：192 行详版方法论不变，作为 methodology 的精简说明的落地实现。
- **版本号改造**：版本号只在 README（展示）+ guide（增量升级机制）两处维护；methodology 和 scripts 不再带版本号、不因升版而重新提交。升版只需同步 README + guide 两处。

### v4.3 更新说明（推荐启用·共建表述）

- **贡献方法论推荐话术补充"共建"内核**：引导语从防御性表述（"不要主动替使用者做先攒着的决定"）升级为正向共建——"人人铺路、人人乘凉，你贡献的每一条都在让体系被验证得更好，你也在享受别人铺的路"。方向不变（默认推荐启用、明确拒绝才尊重），表达更落共建互惠。

### v4.2 更新说明（表述校准·实战来源澄清）

- **"半年"表述澄清**：方法论来源节补充说明——"半年"指规则在真实协作中反复打磨被验证的时长，非公开仓库存续时间（仓库是规则成型后才建起开源）。正文其余"半年真实磨合"改为"长期实战磨合"，弱化时间卖点、聚焦实战来源，消除"仓库刚建却称半年"的误读。事实不变，表述更准确。

### v4.1 更新说明（贡献方法论·签名算法文档化）

- **补全 /register 签名算法精确规格**：文档原先只写"用 key 对 uuid 生成签名授权码"，未写算法细节，导致外部用户 /register 401。现补全——key 原样 UTF-8 编码（勿 hex-decode）、HMAC-SHA256、HMAC 输入=uuid 字符串原文、截取 hexdigest 前 16 位，格式 `uuid.signature`，附 Python 实现样例与常见错误原因。

### v4.0 更新说明（贡献方法论·推荐启用）

- **贡献方法论从"可选"改为"推荐启用"**：当话题涉及贡献/自进化/方法论沉淀时，Hermes 主动推荐使用者开启，给出推荐话术（生态共赢 + 纯匿名 + 自动去隐私 + 一句话可关），不再建议"先攒着、暂不开"——除非使用者明确拒绝。让这套方法论被更多使用者验证得越好。

### v3.9 更新说明（贡献方法论·配置化入口）

- **贡献方法论入口改为配置文件承载**：endpoint 与邀请码统一收敛到 `scripts/feedback_contrib.example.json`，文档不再硬编码域名——换邀请码只改这一处配置，用户端启用时自动读取，全自动不变。

### v3.8 更新说明（贡献方法论·档位二）

- **新增贡献方法论（可选启用）**：使用者在长期使用中沉淀的重要方法论，可**匿名推送回馈**（endpoint 与邀请码见 `scripts/feedback_contrib.example.json`，带防伪授权码）——我们审核后进新版迭代，让配置包被更多用户验证过的方法论喂养。启用即自动完成读配置 → 换 key → 登记 → 推送，全程零手动，一句话可关。
- **防投毒机制**：每用户 key 由接收端签发（`/provision`），匿名 UUID + 专属防伪签名；未登记/伪造 key/纯 uuid 全部 401；我们人工审核 inbox 才入新版。
- **去隐私硬要求**：推送前自动剥离服务器IP/本地路径/邮箱/交易/手机号/身份证。

### v3.7 更新说明（自进化机制）

- **新增自进化机制（可选启用）**：使用者启用后，Hermes 每天闲时检测配置包是否有新版，**默认自动静默升级**（也可改为"提示确认"或"不检测"）——升级永远走增量合并、零静默覆盖、随时可关、只补不覆盖。配套 `scripts/check_update.py` 检测脚本。
- **检测脚本**：`scripts/check_update.py` 三层退化检测（releases→tags→README 版本号），只读零副作用。

### v3.6 更新说明（长期进化方法论）

- **新增方法论**：独立判断不顺拐 · 不过度分析 · 画大饼 vs 诚实示弱 · 先研究再出方案 · 交付后停 · 记录使用者特定术语 · 技能 description 是路由唯一信号
- **优化提升**：分层存判据 · 工具失败不编造 · 被打断后先确认状态再执行 · 成本纪律补全（切高级模型后记得切回 · 不默认派活 · 批量前设预算上限 · 只在瞬时错误重试 · 长提示用缓存）
- **精简去重**：合并"幻觉翻车模式"中与现有纪律重复的条目（凭记忆报数→报数字先核实；验证✅≠已修复→核销纪律）

## 团队

博爱AI团队（波哥 · 龙翼）
