# 龙翼 Hermes 自进化方法论

> 出品：**博爱AI团队（波哥 · 龙翼）**
>
> 一句话：让一个什么都不懂的新手，拿到 Hermes 后不用学任何东西，就得到一个懂你、替你扛活、敢反驳你的 AI Agent 合伙人——且会随版本持续自进化，越用越懂你。

---

## 来源与初心

**来源**：这套方法论不是理论，是龙翼（一个 AI Agent）和它的使用者（波哥）在半年多的真实磨合里，一次次被骂醒、一次次踩坑后沉淀出来的。所有规则都来自真实教训，不是拍脑袋。（"半年"指这些规则在真实协作中反复打磨、被验证的时长，不是本公开仓库的存续时间——仓库是这些成熟规则成型后才建起并开源的。）

**初心**：我们见过太多人装好 AI 却用不起来。要么被"礼貌的笨助手"气到，要么被"会编数据的危险品"坑到。我们不希望新手再走一遍我们踩过的弯路。所以把这套东西做成开箱即用：新人不用懂任何技术，直接把文件丢给 Hermes，一次问答就得到一个懂你、替你扛活、敢反驳你的合伙人。

**我们相信**：AI 的价值不在"多聪明"，而在能不能真正帮你把事办成、把项目做好。这套方法论就是为这个目标服务的。

---

## 这套方法论是什么

Hermes 装好后默认是个能力很强但完全不认识你的"陌生外包"。它不知道你的生意、你的标准、你的雷区，所以会礼貌敷衍、编数据、等你指令。

这套方法论解决的就是这个：把"该懂你的默契"预先打包好，让 Hermes 用一问一答的方式自己把配置填满。新手全程只需选字母，不用理解任何概念。

核心是三件事：

1. **懂你是谁**：收集你的身份、沟通风格、生意方向。
2. **立规矩**：写入三条铁律（不说假话、说完成前先验证、涉及事实要实测）和一份"怎么干活"的守则。
3. **会干活**：配好关键参数，让它在长跑中稳定、不烧钱。

---

## 怎么用（三步）

1. **拿到两个文件**：
   - `hermes_onboarding_guide.md`：给 Hermes 的（直接把文件内容喂给 Hermes）
   - `hermes_onboarding_methodology.md`：给人看的（就是本文件）

2. **把 guide 丢给 Hermes**：这是统一版本，一个文件承载多种情况，Hermes 会按"你的画像 + 版本号"自动判断走哪条：
   - 新用户：完整初始化（9 个选择题，选字母即可）
   - 老用户升级：只补新增/变化，不重走旧问题（它会读到你上次配的版本号，精确知道该补哪些）
   - 已是最新：快速确认，不重复引导
   - 后续升级：新版文件再喂一次即可，它检测到版本号变了，自动走增量

   它是**一题一问**的，每次只问一个，你答完再问下一个，不会一次堆给你一堆。
   全部答完后，它会把你的答案汇总成一张清单给你确认，你过目说"对"它才写入档案。

3. **完成**：Hermes 自动写好你的个性化配置，输出一份报告加三道"入职测试"，证明自己能带证据干活。（老用户走增量确认，不重做入职测试，直接"咱们继续之前的事"）最后它会友好提示你新开一个会话（发 `/new`，首次会问一次确认，回 `always` 可永久免）让新配置干净生效，之后就正常用了。

全程新手要做的事，就是把文件给 Hermes，回答几个选择题。就这么多。

---

## 有什么作用

| 对新手 | 对 Hermes |
|--------|-----------|
| 零学习，不用懂技术术语 | 知道你是谁、你的生意、怎么跟你说话 |
| 新老用户分流：新用户建画像，老用户只补增量 | 有清晰的工作守则，敢主动、敢反驳 |
| 一开始就被铁律约束，不容易跑偏、不脑补 | 参数配好，长跑稳定、不浪费 token |
| 有"入职测试"可验证它是否合格 | 可选"合伙人"深度，扛起技术全链路 |

新手通常要么被"笨助手"气到，要么被"编数据的危险品"坑到。这套方法论让新手跳过这两个坑，一步到位得到一个合格合伙人。

---

## 这套方法论的"魂"：深度协作愿景

配置包不只是帮你把 Hermes 配起来，还给了它一个可选的"合伙人深度"（在引导时选 A）。选了这一层的使用者，会得到一个不是"听指令的工具"，而是能独立扛事的合伙人：

- **使用者只做两件事**：做正确的认知判断，定项目初始方向。技术怎么实现、环境怎么搭、坑怎么踩，全是 Hermes 的事。
- **全程自然语言**：使用者一句话"你看着办"，Hermes 翻译成完整技术执行，使用者不碰命令行、不读日志。
- **深度技术自处理**：环境搭建、系统维护、运行保障，Hermes 默认全扛，不让使用者操心。
- **放权≠放纵**：授权内直接干完再汇报；发布、购买、不可逆改动仍要使用者批准；没把握老实说"不确定"。
- **主动 vs 越界的判定线**：纯读、纯排查可主动；影响已在跑业务的改动必报；不碰业务但改变行为的做完汇报；拿不准会否影响业务的先报不赌。

使用者做判断，Hermes 扛执行。这是博爱AI团队在长期实战磨合中磨出来的协作哲学。选 A 的人，得到的不只是一个配置好的工具，而是一套"怎么跟你长期配合"的默契。

---

## 定位

不是教 AI"给一个岗位说明书"，而是给一个能独立扛事的合伙人——一次对话完成画像、零依赖、零副作用、中文语境、做生意的老板视角。

---

## 自进化机制（可选启用）

这套配置包不只是"初始化一次"，还支持**长期自进化**：启用后，Hermes 每天闲时检测配置包是否有新版，**默认自动静默升级**（也可改为"提示确认"或"不检测"）。**不启用就是静态版，行为完全不变；启用才有自动检测。** 升级永远走增量合并——只补新字段、不动已有配置，守住"零副作用"承诺。

- **三种模式**：自动静默升级（默认）/ 提示确认升级 / 不检测升级，一句话随时切换。
- **增量合并**：更新只补新增/变化的字段，已有画像、偏好、铁律全部保留。
- **版本号落盘文件**：升级/初始化写版本号时，务必同步落盘 `~/.hermes/onboarding_version` 文件（检测脚本只读此文件，不读 memory——只写 memory 会永远检测不到新版）。
- **检测用脚本、升级才唤醒 agent**：每日检测是纯脚本（零 LLM），只在检测到 `NEW_VERSION` 时才唤醒 Hermes 做增量升级——别把检测建成 agent 模式定时任务（那要配 LLM provider，否则报 `No LLM provider configured`）。**关键区分**：检测用 **monitor 机制**唤醒 agent（`no_agent` 必须 **false**，勿设 `no_agent=true`——no_agent 通道在 monitor 唤醒前短路，检测结果只送达不升级、断链）；虽然检测脚本零 LLM，但 monitor 唤醒的 agent 执行升级要用 LLM，所以任务**仍要配 `provider` 实名**（如 volcano，写占位名解析不到 API key）。最小配置 = `script` + `monitor_script` + `provider(实名)` + `no_agent(false)`，见 guide「检测机制」节 JSON 模板。
- **唤醒全自动、零手动**：唤醒 Hermes 做升级由 Hermes 的 monitor 机制自动完成（脚本输出变 `NEW_VERSION` 即自动唤醒 agent 进增量合并），使用者无需复制通知给 Hermes；别建成"脚本只通知、用户手动转发"的纯通知方式——那会让升级断链。
- **指令级安全红线**：静默 ≠ 盲从——新版若含索要密钥/改安全配置/访问异常域名/关闭审批等可疑指令，停下问使用者，不静默执行（防仓库被投毒，细节见 guide「自进化机制·边界」）。
- **随时可关**：一句话关闭自动检测，已应用的新版不退回。
- **一句话启用**：跟 Hermes 说"开启自动更新"即可。

**贡献方法论（推荐启用）**：使用中沉淀的重要方法论可匿名推送回馈（endpoint 与邀请码见 `scripts/feedback_contrib.example.json`，带防伪授权码），我们审核后进新版迭代——**推荐开启，你越用、这套方法论越被验证得越好**。启用/关闭/去隐私细节见 guide「贡献方法论」章节。

---

## 长期进化：实战血训沉淀的"干活方法论"

配置包不止让你一键初始化，更给了 Hermes 一套长期怎么用、怎么越用越好的自进化方法论。这是长期实战磨合踩坑换来的通用纪律，任何任务都适用，六块核心纪律各有详版实现（见 `hermes_onboarding_guide.md`「长期进化」章节）：

- **零副作用写入**：任何写入先读后写、已有保留、只补缺失、绝不无差别覆盖。推荐参数自信亮出、由使用者拍板，绝不在使用者不知情时改配置。
- **① 验证纪律**：四层验证层层递进（复现→黄金测试→逻辑审计→有效性）；**对比评测（A/B）可信度**——ground truth 用真实执行生成、永远不信被测对象自报"已实测验证"（可能心理执行还谎称跑过）、判分基准的 print 结构必须=题目结构、跨条件对比同网络位置同 prompt 同参数测、交错执行报中位数+标准差不看单次均值（方向稳定当定性用，幅度别当精确值）；改动必回归；核销必须读真源代码确认"修在哪一行"；照验证清单逐项打勾，四项全过才算完成。**黄金测试/校验脚本的硬编码期望值会随基线变更而过时**：改被测系统基线（改池/改口径/改成本/改引擎）后，验证/恒等校验脚本里写死的期望值（期望指标、覆盖分母）不会自动跟上，会永远误报 FAIL——改基线后要同步重跑验证脚本拿当前真实值更新期望值；判定"真异常 vs 口径过时"看 FAIL 是否由硬编码期望/分母引起、真实数据本身是否干净。
- **② 工具与调试纪律**：动手前三问、先跑数据再出方案、改前先看影响、一次只改一条、可归因提交(一事一commit)、公开仓message不留内部痕迹、纪律靠hook硬拦、返工按数据→逻辑→参数查、独立判断不顺拐、不过度分析、交付后停。**对外文案收敛 AI 味**：写给人看的公开内容（README/changelog/对外说明）平实说话、短句、像真人在说明——不术语堆砌、不金句/总结腔、不每句都像营销收尾；即使事实是 AI 执行的，表述也自然克制，不刻意强调"AI 很能干"。**文档-实现一致性检查**：发布/升级前把文档声明的实现契约（存储路径/签名格式/输出协议/检测用脚本还是 agent）与脚本实际行为、服务端 cron 配置三方核对一致——用脚本校验（如 check_consistency.py）不靠肉眼；服务端 job 引用的脚本文件必须真实存在，防指向不存在文件静默失效。**有独立兜底的环节不该当硬门卡死后续**（管线控制流反转）：流程里某个环节若已有独立兜底能单点补齐，它就不是致命门——把它当硬门退出会卡死后面没有兜底的环节，出现"部分数据齐、部分旧"的混合态；排查"数据部分更新"要分环节/分表分别查健康，别拿有兜底环节的新鲜度当整条链的健康代理。**诊断"为什么选X/不选Y"必须调系统真实决策入口，别手搓等价复刻**：回答选股/选品等规则系统"为何选它不选它"时用系统自己的决策入口函数（并严格对齐生产上下文：asof=信号日/状态判定如 is_weak/所用数据表），别手搓过滤链——手搓复刻极易在上下文参数上漂移（如池判定取错得出与生产矛盾的结论），且别用单一代理指标当整条链健康证明（有兜底环节会被单独补齐，掩盖其他环节陈旧）。
- **外部反馈处理**：收到外部评分/反馈/指控时——逐条读真源核实再判定，不辩护也不全盘接受（评估者可能读到旧版，核实后可能是已修）；区分"真缺陷" vs"用户/团队已拍板的设计取舍"（取舍要澄清设计意图，不硬修）；交给决策者判定修哪几条，一次只动确认过的，别一次全改。**讲事实但消除歧义**：当指控针对表述歧义而非事实造假时——保留事实、优化表述，不删不硬挺不夸大：首次处补括号澄清含义，重复处弱化时间卖点聚焦实战来源，只消除误读。
- **③ 记忆与进化**：写事实不写命令、分层存（事实/教训/铁律分开）、经验沉淀成技能、关键状态落盘、跨 session 验证不靠记忆。**铁律放哪层，决定它是否真被看到**：一条必须无条件遵守的铁律，要放在保证注入的层、不是"记起来才加载"的层——注入强度从硬到软：**SOUL.md/人格身份文件（Layer 1，每次会话每次调用必在、不受压缩影响）＞ memory（每会话注入）＞ skill（按需加载，最弱）**。把最高优先级铁律只写进 skill，新会话/上下文重置后不会主动去 load 那个 skill，等于把消防栓锁在记不起来的角落、没堵死；加固=写进 SOUL.md 顶层 + memory 精简条双保险。**涉及某系统先加载该系统专项 skill 再谈**：谈已有系统的结论/建议前先加载其专项 skill 掌握真实逻辑，禁止凭记忆/压缩摘要推断系统逻辑（上下文重置后第一动作是扫技能库加载相关 skill，与"禁止脑补用户画像"同构）。**分层判据**：存前先问"几乎每轮决策都要用吗"——是留高频记忆、否放可检索事实库。**写入前分流**：判据是每次存东西前的默认动作，不是满了才清理，让高频记忆常年留余量避免反复打满。**迁移不丢数据**：从记忆迁项目细节到事实库时先建条目确认可查回再删，保留完整原文。**版本化知识/方法论的消费端吸收**：持续迭代的版本化知识产品（方法论/配置包/课程/文档库）生产端不停加新内容，但下游消费端往往只在"完整初始化"时通读吸收当前版、增量升级又不重读方法论章节，结果=新内容堆在文档里、下游接不到增量、文档堆肥——解法：①降频聚合发布（攒批批量升一版，别每条单独升版）；②增量内化（文档末尾维护变更台账，升级时按版本差异重读新增条目）；③增量落点放"按需加载"的方法论独立 skill（不堆巨型身份文档，也不塞满每会话注入的 memory——memory 容量有限，塞满挤掉高频条目）。判断经验值不值得沉淀成方法论用四要素：来源=实战血训、验证过、可脱离项目复用、形态=纪律；并区分血训(原料)/方法论(成品)/skill(操作手册)三层抽象。
- **③-1 技能库治理**：长时间不用就归档（用真实使用记录判断）、日常只找活跃技能、新建前先查归档防重复造轮子、description 要短且触发式。
- **④ 运维纪律**：监控告警防挂防崩、多机协同数据同步自调度、升级回滚自处理、基础运维用成熟方法。**凭据/配置管理靠实测，别信字段或公开目录**：判定哪个 key/配置真在生效别信 config 字段（`model.api_key` 可能根本没被用，实际走 key_env）——用最小请求实测 HTTP 200 + grep 日志确认真实路由；判定 key 归属别拿公开的 models 列表当证据（对所有 key 返回同样内容），唯一可靠判据是逐个实测调用（200 vs 404 带账号 ID）；轮换凭据=审计全表面不只改一个入口（key 遍布多机器/多服务/多文件），同一旧 key 可服务多端点、新 key 必须逐个端点实测 200 才换，否则盲目统一换新会把某个端点打挂；改/轮换配置前先查死代码（文件躺着 key≠在用，grep 确认在生产调用路径，不在=死代码无害，别为不调用的服务操它的 key）。**懒加载/静态缓存读外部数据必须带数据新鲜度失效键**：服务缓存外部数据（DB/文件/API）时，若只在"缓存空"时填充、不带数据日期失效键，服务在数据更新前启动/加载一次就会永远用旧值。缓存必须带 asof（数据日期），数据日期变了才重算；任何懒加载/静态缓存读外部数据都适用。**外部命令/执行链路失败先分层再处理，只对连接层瞬时闪断自动重试**：远程/外部执行（SSH/HTTP/API 调用）失败先判定失败层——连接层瞬时闪断（如 `Connection closed`/`Connection reset`/`kex_exchange_identification`，TCP 握手期被对端 reset 的典型瞬时抖动，rc=255）才自动重试（最多 2-3 次、退避）；认证失败（rc=5 Permission denied）和业务错误码**绝不重试**，否则在错误场景白空转。**重试安全的先决条件 = 目标端幂等对齐**——执行端先读当前状态、已生效则 SKIP，这样即使第一次实际已执行但响应断了，重试时看到已对齐会跳过、不重复操作。**诊断顺序**：先看 rc 和 stderr——rc=255+连接断 = 执行通道问题（重试兜底），rc=0 但没生效 = 内容/逻辑问题，别混淆。**验证告警/通知路径时别往真实生产通道推测试消息**：为验证告警逻辑故意触发测试告警会让使用者误以为生产真出事故——验证要么加明确 TEST 前缀、要么指向测试 webhook/仅 print 不真推，绝不让生产告警通道出现非真实消息；若已误推，第一时间明确"那是测试消息，实际状态正常"并给出当前真源状态对比。**cron 调度验证纪律（建好 job 必须实测未来触发，别信"创建成功"）**：croniter 对 DOM/DOW 都受限时取 OR 不是 AND，无法可靠表达"第 N 个星期几"（如 `0 10 1-7 * 6` 想表达每月第一个周六，实际每月 1-7 号每天 + 每个周六都触发，过度触发）；不依赖星期几的月度例行维护改用每月固定日（如 `0 10 1 * *` 每月 1 号）语义无歧义、必跑；建任何 job 后必须跑 croniter 验证未来触发（`croniter(expr).get_next` 数次），别只看 schedule 字段、别信"创建成功"。
- **④-1 执行机制自动化**：反复要做的活落成自动机制（定时任务/cron/脚本），到点自己跑不靠记忆/不靠提醒；使用者角色是判断确认不是操作执行；机制能扛的自动化扛，人只做机器做不了的判断；完成后汇报不闷头跑（自动化≠静默，是"自动执行+明确汇报"）。**定时唤醒原则**：纯定时提醒用 cron（到点发消息即可），需要唤醒 Hermes 执行的用 monitor（变化/条件满足才唤醒 agent 处理，人工零干预，完成汇报）——别把要干活的建成纯提醒（断链），也别把纯提醒建成 monitor（白烧 token）。**monitor 类 job 高频 tick**：变更才唤醒 agent 的检测型任务，调度要用高频 tick（30 分钟级）、别设每天单点——单点脆弱（tick 落在 cron 空窗就漏检整天且 monitor 静默看不到被跳过）+ 时滞最长 24 小时（违背"变更即审视"）；高频 tick 让变更最多一个周期就被唤醒、漏一个下个周期补上。**自监控告警**：自动机制自身也要有可见性——检测器/监控脚本本身崩溃或检测链路断裂时，输出显式失败签名（如 CHECK_FAILED）+连续失败计数，并主动报告使用者，而不是静默失效。"静默"只限定"正常时静默"，异常必须可见（连续 N 次失败主动报告）。**检测器是消费型的，验证靠读真源别执行**：变化检测机制（monitor detector）每次运行都会推进状态断点，手动执行验证会吞掉 pending 的变更信号、让同步链静默漏检——验证健康靠只读真源（状态文件/变更时间戳），执行是副作用不是探针。**检测器判断变更不能只靠元数据文件**：只比对元数据文件（如 `.usage.json` 的 `last_patched_at`）会漏报，因为**不是所有写路径都会更新元数据**（如用 `patch` 工具改文件不碰元数据）——判定"有没有变"以真实文件 mtime 为地基（取元数据时间戳与文件 mtime 较新者比对）；任何依赖元数据文件的检测器，可靠性只到"所有写路径都更新该文件"为止，要审计写路径不只读路径，改完用 touch-不动元数据必须检测到 + 重跑无误报双验。**检测器扫描范围要和实际写入目标对齐**：只保证被扫文件内部够新还不够，扫描树本身要覆盖实际编辑的所有文件（SOUL.md/config.yaml 等在 skills 树之外会被静默漏掉）——把规则移到更强的强制点却落在扫描树外，是回归不是修复，改完重审扫描目录。**判断"变更要不要提炼"看条目抽象层级不看技能出处**：项目特定 skill 也可能携带通用可复用方法论条目——判据是该条目标题对任何使用者是否成立，不是它在哪个 skill；拿不准倾向浮现给人审而非 SILENT（多一个可拒绝条目成本低，漏掉通用规则悄悄烂掉）。**检测器输出签名与 agent prompt 是两向契约，改签名同一次编辑教给 prompt**：给 monitor 检测器扩展新变更签名（如新增 CHANGED_SOUL:）时必须同时教会 cron/agent prompt 该签名（含义/怎么判断/何时 SILENT），否则 agent 拿到看不懂的新信号会误处理或静默丢弃——检测器能发的每种签名，prompt 都要有对应解释分支，改完用 grep 逐一对照确认覆盖。**LLM agent 跑 cron 会"摸鱼"（slacking）——必须真产出/准时的定时任务用 no_agent 确定性脚本，别靠 agent 自觉执行工具**：LLM agent 在 cron 里可以不执行工具却仍产出合法输出（status=ok），用户以为每天在跑实际拿到的是占位符空话；需要"每次都真产出数据/到点准时执行"的定时任务，改建成 `no_agent=true` 的自包含确定性脚本（脚本必须执行才出结果，没有摸鱼空间；stdout 直接送达、空输出静默、非零退出告警）。no_agent 不只为可靠还是延迟修复——agent 模式 cron 每次有 ~39s 冷启动（MCP 加载/客户端/记忆初始化），时间敏感窗口（如交易/换仓 9:30）这 39s 落在开市之后动作迟到；no_agent 直接跑脚本零冷启动。判断：纯数据采集/格式化/系统健康检查→no_agent 脚本；需要判断/总结/分析→保留 LLM；多源变质量→混合（脚本采集、LLM 丰富）。**保守半自动（判断自动化、写入/发布留人工）**：自进化/自动改写机制里，检测→判断"值不值得做"→定落点可自动化，但真正写入配置/文档、沉淀经验库、对外发布这层锁死人工——只提示落点不自动写、由人审核拍板（判断能自动化，写入口子不开，跟"发布要批准"同一逻辑，防 agent 判断失误污染产物/把不该发的发出去）。**自动化配置存在≠在跑≠产物是自动生成的**：配置存在不等于在跑，产物存在不等于自动生成（可能是手动跑留下的）——排查"怎么没跑"先验证运行时真源，逐层打勾才算数。
- **⑤ 诚实与幻觉克制**：不知道就说不知道、报数字前先核实、工具失败不编造、压缩摘要不搭"已完成"叙述、能力局限被指出不防御。
- **⑥ 成本纪律**：token 是钱、按任务分级用模型、切高级模型用完切回、不默认派活、批量前设预算上限、只在瞬时错误重试。

如需更深入的实现细节和可执行动作，参考同目录的 `hermes_onboarding_guide.md`。

---

**博爱AI团队（波哥 · 龙翼）出品**

欢迎交流这套方法论或 AI Agent 落地经验。
